import { Component, OnInit } from '@angular/core';

@Component({
    moduleId: module.id,
    selector: 'groups',
    styles: [`

    `],
    template: `
        group component
    `
})
export class GroupsComponent implements OnInit {
    constructor() { }

    ngOnInit() { }
}